services:
  bug-tracker:
    build: .
    ports:
      - "8002:8000"
    volumes:
      - bug_db:/app/data
    environment:
      - DB_PATH=/app/data/bugs.db
    restart: unless-stopped

volumes:
  bug_db:
